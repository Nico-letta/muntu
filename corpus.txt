import React from 'react';
import { View, Text } from 'react-native';

const MobileMoneyPayment = ({ provider, amount }) => {
  const handlePayment = () => {
    if (provider === 'MTN' || provider === 'Airtel') {
      console.log(`Initialisation du paiement MoMo de ${amount} XAF`);
    }
  };
  return (
    <View style={{ padding: 20 }}>
      <Text>Montant à payer : {amount}</Text>
    </View>
  );
};